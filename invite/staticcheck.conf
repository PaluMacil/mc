checks = [
    "all",
    "-ST1000",   # missing package comment, noisy for cmd/ and internal/
    "-ST1003",   # initialisms; collides with generated code and JSON-shaped structs
    "-ST1013",   # HTTP status code constants; inline numerics are fine
    "-ST1020",   # doc comment must start with func name
    "-ST1021",   # doc comment must start with type name
    "-ST1022",   # doc comment must start with var name
    "-SA1019",   # deprecation; gate elsewhere or fix opportunistically
]

dot_import_whitelist = []