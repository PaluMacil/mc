// mc-web: the mc.danwolf.net landing page. Deliberately stdlib-only (house
// style: a tiny Go binary with embedded assets), so the image stays minimal
// and there is nothing to keep patched.
module github.com/PaluMacil/mc/web

go 1.26
