<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>All the Mods 10 server</title>
<meta name="description" content="How to install and connect to our All the Mods 10 Minecraft server, written for players and their parents.">
<style>
  :root {
    --bg: #14161a;
    --panel: #1e2128;
    --panel2: #262a33;
    --text: #e8eaed;
    --muted: #a7adba;
    --accent: #5db462;
    --accent2: #7fd884;
    --border: #333844;
    --warn-bg: #3a2c14;
    --warn-border: #7a5a1e;
    --warn-text: #f4d58b;
    --code: #0f1115;
  }
  @media (prefers-color-scheme: light) {
    :root {
      --bg: #f4f6f8;
      --panel: #ffffff;
      --panel2: #eef1f5;
      --text: #1b1e24;
      --muted: #566072;
      --accent: #2f8f43;
      --accent2: #257035;
      --border: #d7dde5;
      --warn-bg: #fff5db;
      --warn-border: #e2b657;
      --warn-text: #6b4e12;
      --code: #eef1f5;
    }
  }
  * { box-sizing: border-box; }
  body {
    margin: 0;
    background: var(--bg);
    color: var(--text);
    font: 16px/1.6 system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, sans-serif;
  }
  .wrap { max-width: 820px; margin: 0 auto; padding: 2rem 1.1rem 4rem; }
  header { text-align: center; padding: 1.5rem 0 0.5rem; }
  h1 { font-size: 2rem; margin: 0.2rem 0; letter-spacing: 0.5px; }
  h2 { font-size: 1.35rem; margin: 2.4rem 0 0.8rem; }
  h3 { font-size: 1.05rem; margin: 0 0 0.3rem; }
  .tagline { color: var(--muted); margin: 0.2rem 0 0; }
  .guidelinks { text-align: center; color: var(--muted); margin: 1.6rem 0 0; }
  .card {
    background: var(--panel);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 1.2rem 1.3rem;
    margin: 1.2rem 0;
  }
  .connect { text-align: center; }
  .addr {
    display: inline-flex; align-items: center; gap: 0.6rem;
    background: var(--code); border: 1px solid var(--border);
    border-radius: 10px; padding: 0.7rem 0.95rem; margin: 0.4rem 0;
    font-size: 1.35rem; font-weight: 600;
  }
  .addr code { font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
  .copy {
    font: inherit; font-size: 0.8rem; font-weight: 600; cursor: pointer;
    background: var(--accent); color: #06210b; border: 0;
    border-radius: 7px; padding: 0.35rem 0.6rem;
  }
  .copy:hover { background: var(--accent2); }
  .fallback { color: var(--muted); font-size: 0.95rem; margin-top: 0.4rem; }
  .fallback code { background: var(--code); padding: 0.1rem 0.35rem; border-radius: 5px; }
  .badge {
    display: inline-block; background: var(--accent); color: #06210b;
    font-weight: 700; border-radius: 999px; padding: 0.25rem 0.8rem; font-size: 0.95rem;
  }
  .btns { display: flex; flex-wrap: wrap; gap: 0.7rem; justify-content: center; margin-top: 1rem; }
  .btn {
    display: inline-block; text-decoration: none; font-weight: 600;
    border-radius: 9px; padding: 0.7rem 1.1rem; border: 1px solid var(--border);
    color: var(--text); background: var(--panel2);
  }
  .btn.primary { background: var(--accent); color: #06210b; border-color: transparent; }
  .btn:hover { filter: brightness(1.08); }
  .note {
    background: var(--warn-bg); border: 1px solid var(--warn-border);
    color: var(--warn-text); border-radius: 10px; padding: 0.8rem 1rem; margin: 1rem 0;
  }
  ol.steps { counter-reset: step; list-style: none; padding: 0; margin: 0; }
  ol.steps > li {
    position: relative; background: var(--panel); border: 1px solid var(--border);
    border-radius: 12px; padding: 1.1rem 1.2rem 1.1rem 3.4rem; margin: 0.9rem 0;
  }
  ol.steps > li::before {
    counter-increment: step; content: counter(step);
    position: absolute; left: 1rem; top: 1.05rem;
    width: 1.7rem; height: 1.7rem; border-radius: 50%;
    background: var(--accent); color: #06210b; font-weight: 700;
    display: flex; align-items: center; justify-content: center;
  }
  ol.steps p { margin: 0.4rem 0; }
  .shot { margin: 0.8rem 0 0.2rem; }
  .shot img { max-width: 100%; height: auto; border-radius: 10px; border: 1px solid var(--border); display: block; }
  .shot figcaption { color: var(--muted); font-size: 0.9rem; margin-top: 0.4rem; }
  ul.rules { padding-left: 1.2rem; }
  ul.rules li { margin: 0.35rem 0; }
  code { font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
  .em { color: var(--accent2); font-weight: 600; }
  footer { text-align: center; color: var(--muted); font-size: 0.9rem; margin-top: 3rem; }
  footer a { color: var(--muted); }
  a { color: var(--accent2); }
  .topnav { display: flex; gap: 1.1rem; flex-wrap: wrap; padding: 0.2rem 0 1.4rem; }
  .topnav a { color: var(--muted); text-decoration: none; font-weight: 600; }
  .topnav a:hover { color: var(--text); }
  .topnav a.signedin { color: var(--accent2); }
  .statusbar { display: flex; align-items: center; gap: 0.5rem; flex-wrap: wrap;
    background: var(--panel2); border: 1px solid var(--border); border-radius: 10px;
    padding: 0.6rem 0.9rem; margin: 1rem 0 0; font-size: 0.95rem; }
  .dot { width: 0.6rem; height: 0.6rem; border-radius: 50%; background: var(--accent); display: inline-block; }
  .dot.off { background: var(--muted); }
  .pcount { font-weight: 700; }
  .pnames { color: var(--muted); display: flex; gap: 0.35rem; flex-wrap: wrap; align-items: center; }
  .pchip { background: var(--panel); border: 1px solid var(--border); color: var(--text);
    border-radius: 999px; padding: 0.05rem 0.55rem; font-size: 0.85rem; }
  .statusbar .maplink { margin-left: auto; }
</style>
</head>
<body>
{{template "topnav" .}}
<div class="wrap">

  <header>
    <div class="badge">All the Mods 10 (version {{.PackVersion}})</div>
    <p class="tagline">A small, whitelist-only Minecraft server for friends and their kids.</p>
  </header>

  <section class="card connect">
    <h3>Connect</h3>
    <div class="addr">
      <code id="addr">{{.ServerAddress}}</code>
      <button class="copy" data-copy="{{.ServerAddress}}" type="button">Copy</button>
    </div>
    <p class="fallback">If your launcher will not connect, use the explicit address <code>{{.FallbackAddress}}</code> instead.</p>
    <div class="btns">
      <a class="btn primary" href="{{.MapPath}}">Open the live map</a>
      <a class="btn" href="{{.CurseforgePath}}">How do I get on?</a>
    </div>
    <div id="players" class="statusbar"><span class="pnames">Checking who is online&hellip;</span></div>
    <p class="fallback">The server is whitelist only. You have to be added before you can join; ask Dan.</p>
  </section>

  <p class="guidelinks">
    New here? Read the <a href="{{.CurseforgePath}}">CurseForge install guide</a> (easiest), the
    <a href="{{.VanillaPath}}">vanilla launcher guide</a>, the <a href="{{.TipsPath}}">ATM10 tips</a>,
    and the <a href="{{.RulesPath}}">house rules</a>.
  </p>

  <footer>
    <p>Inviting a friend, or new here? <a href="{{.PortalPath}}">Sign in to the member portal</a>.</p>
    <p>Ping problems or a blocked connection? See the <a href="{{.ParentsPath}}">parent tips</a>.</p>
    <p>&copy; {{.Year}} &middot; a homelab All the Mods 10 server</p>
  </footer>

</div>

<script>
  // Progressive enhancement: copy-to-clipboard. If the API is missing or the
  // page is served without JS, the address is still plainly visible above.
  document.querySelectorAll(".copy").forEach(function (b) {
    b.addEventListener("click", function () {
      var text = b.getAttribute("data-copy");
      if (!navigator.clipboard) return;
      navigator.clipboard.writeText(text).then(function () {
        var old = b.textContent;
        b.textContent = "Copied";
        setTimeout(function () { b.textContent = old; }, 1200);
      });
    });
  });

  // Live "who's online": fetch the same-origin fragment the portal serves and
  // poll it. Degrades to the placeholder text if the portal is unreachable.
  (function () {
    var box = document.getElementById("players");
    if (!box) return;
    function refresh() {
      fetch({{.PlayersURL}}, { headers: { "Accept": "text/html" } })
        .then(function (r) { return r.ok ? r.text() : Promise.reject(); })
        .then(function (html) { box.innerHTML = html; })
        .catch(function () {});
    }
    refresh();
    setInterval(refresh, 20000);
  })();
</script>
{{template "authscript" .}}
</body>
</html>
